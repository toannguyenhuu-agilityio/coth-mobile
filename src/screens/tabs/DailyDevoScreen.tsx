import { Text, View } from 'react-native';

export const DailyDevoScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Notes List</Text>
    </View>
  );
};
