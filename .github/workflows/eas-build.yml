name: Build COTH App

on:
  push:
    branches:
      - develop
      - staging
      - main

jobs:
  eas-build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    defaults:
      run:
        working-directory: apps/coth-mobile

    env:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
      EXPO_PUBLIC_EAS_PROJECT_ID: ${{ secrets.EXPO_PUBLIC_EAS_PROJECT_ID }}
      EXPO_PUBLIC_SENTRY_DSN: ${{ secrets.EXPO_PUBLIC_SENTRY_DSN }}

    steps:
      # 1️⃣ Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js and enable Yarn via Corepack
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "yarn"

      - name: Enable Corepack
        run: corepack enable

      # 3️⃣ Cache Yarn + Turborepo build outputs
      - name: Restore Yarn and Turbo cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.yarn/cache
            .turbo
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-

      # 4️⃣ Setup Expo and EAS CLI
      - name: Setup Expo and EAS
        uses: expo/expo-github-action@v8
        with:
          eas-cache: true
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      # 5️⃣ Install dependencies
      - name: Install dependencies
        run: yarn install --frozen-lockfile

      # 6️⃣ Determine build profile and environment
      - name: Determine build profile
        id: set-profile
        run: |
          if [[ $GITHUB_REF == refs/heads/main ]]; then
            echo "profile=production" >> $GITHUB_OUTPUT
            echo "env=production" >> $GITHUB_OUTPUT
          elif [[ $GITHUB_REF == refs/heads/staging ]]; then
            echo "profile=staging" >> $GITHUB_OUTPUT
            echo "env=staging" >> $GITHUB_OUTPUT
          else
            echo "profile=development" >> $GITHUB_OUTPUT
            echo "env=development" >> $GITHUB_OUTPUT
          fi

      # 7️⃣ Build iOS app (internal build first)
      - name: 🍏 Build iOS app
        run: |
          echo "Building iOS for ${{ steps.set-profile.outputs.profile }}"
          npx eas-cli build --non-interactive --profile ${{ steps.set-profile.outputs.profile }} --platform ios
        env:
          EXPO_PUBLIC_APP_ENV: ${{ steps.set-profile.outputs.env }}
          EXPO_PUBLIC_SENTRY_DSN: ${{ secrets.EXPO_PUBLIC_SENTRY_DSN }}
          EXPO_PUBLIC_EAS_PROJECT_ID: ${{ secrets.EXPO_PUBLIC_EAS_PROJECT_ID }}

      # 8️⃣ Build Android app
      - name: 🤖 Build Android app
        run: |
          echo "Building Android for ${{ steps.set-profile.outputs.profile }}"
          npx eas-cli build --non-interactive --profile ${{ steps.set-profile.outputs.profile }} --platform android
        env:
          EXPO_PUBLIC_APP_ENV: ${{ steps.set-profile.outputs.env }}
          EXPO_PUBLIC_SENTRY_DSN: ${{ secrets.EXPO_PUBLIC_SENTRY_DSN }}
          EXPO_PUBLIC_EAS_PROJECT_ID: ${{ secrets.EXPO_PUBLIC_EAS_PROJECT_ID }}
